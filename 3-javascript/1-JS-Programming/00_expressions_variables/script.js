// what is data - different types of data
// Different types of operators
// Different operation

// data is resourse which we need in programming

// string = "once i was walking in foreset, then saw a tiger"
// number = 1 2 3 4 -1 -4,
// boolean = true / false
// null = no value
// undefined = js will assigned value to empty varaible or if function not returning value.
// array = [[],[5],4,"string", true, null, undefined, {color:"red", model:"MG4 EV", range:"420 KM", battery:"64KWH"}]
// object = {color:"red", model:"MG4 EV", range:"420 KM", battery:"64KWH"}

// Operators

//  string operator - + , "apple " + "Mango"

// Math - +,-,/,* %

// assignment operator ,  let x = 5, x += 10, x -= 10, x *= 5, x /= 2, x++, x--

// comparison operator = 5 === 5, 5 >4, 5 >= 5, 5 < 4, 5 <= 4,

// Logical operator = !, ||, && , 5 !== 4, 5 > 4 || 5 < 4 , true || false, true && false

// What is varaibles  - Place where we store, hold a data.

let x = { name: "dev", age: 45 };

console.log(x);
